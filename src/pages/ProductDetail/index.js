import { Product, NewRelease } from '~/components/Layout/components/Product';

const ProductDetail = () => {
    return (
        <div>
            <Product />
            <NewRelease />
        </div>
    );
};

export default ProductDetail;
