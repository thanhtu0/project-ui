import classNames from 'classnames/bind';
import styles from './NewRelease.module.scss';

const cx = classNames.bind(styles);

const NewRelease = () => {
    return <div className={cx('wrapper')}>NewRelease</div>;
};

export default NewRelease;
